/**
 * Hints from HTML & CSS3 udemy course
 *
 * 1. if scroll does not work on all brower use javascript (el.scrollIntoView({behaviour: 'smooth'}));
 *
 * 2. IntersectionObserver for obserwing when scrolling reaches particular place on page.
 *
 * 3. backdrop-filter: blur() , and filter: blur(), what's the difference ?
 */
